### Crowls IMDB top 250 films. 

# Assumed Steps

**Build an Image**
```bash
$ docker build -t python-imdb .
```

**Run an Container**
```bash
$ docker run -t -i python-imdb
where:
     "-t" flag - sudo mode
     "-i" flag - interactive mode  
```
